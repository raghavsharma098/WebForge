<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Cards</title>
  <link rel="stylesheet" href="style.css">
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    /* Spark effect */
    #spark-container {
    position: fixed;
    width: 100%;
    height: 100%;
    pointer-events: none;
    }

    .spark {
    position: absolute;
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(255,165,0,1) 0%, rgba(255,69,0,0.8) 50%, rgba(255,0,0,0.5) 100%);
    pointer-events: none;
    transform: translate(-50%, -50%);
    box-shadow: 0 0 8px rgba(255,165,0,0.8), 0 0 15px rgba(255,69,0,0.6);
    animation: spark-animation 1s ease-out forwards;
    }
    @keyframes underline {
        0% {
            width: 0;
        }
        100% {
            width: 100%;
        }
        }

        .underline-animate {
        position: relative;
        display: inline-block;
        }

        .underline-animate::after {
        content: '';
        position: absolute;
        bottom: 0;
        left: 0;
        width: 0;
        height: 2px;
        background-color: #ff6a00; /* Orange underline */
        transition: width 0.3s ease-in-out;
        }

        .underline-animate:hover::after {
        width: 100%;
        }
</style>
</head>
<header class="flex justify-between items-center px-8 py-4">
    <!-- Add underline animation class here -->
    <a href="/" class="text-lg text-orange-400 underline-animate">← Back to Home</a>
</header>
<body class="bg-black text-neutral-50">
    <div id="spark-container"></div>
    <div class="flex justify-center items-center px-8 relative pt-10" id="builder-title">
        <h1 class="font-title text-[50px] font-bold text-center text-neutral-50" style="font-family:serif; color: red;">
          WebForge, 
          <span class="text-neutral-500">Members</span>
        </h1>
        
      </div>
  <% if (users.length === 0) { %>
    <p>No users registered yet.</p>
  <% } else { %>
    <% users.forEach(user => { %>
      <div class="card">
        <div class="img">
          <img src="https://via.placeholder.com/150" alt="User Image">
        </div>
        <div class="user-info">
          <h3><%= user.name %></h3>
          <p>Email: <a href="mailto:<%= user.email %>"><%= user.email %></a></p>
          <p>Phone: <%= user.phone %></p>
          <p>Address: <%= user.address %></p>
          <p>LinkedIn: <a href="https://www.linkedin.com/in/<%= user.linkedin %>" target="_blank"><%= user.linkedin %></a></p>
          <p>GitHub: <a href="https://github.com/<%= user.github %>" target="_blank"><%= user.github %></a></p>
        </div>
      </div>
    <% }); %>
  <% } %>
<script src="card.js"></script>
<script src="script.js"></script>
</body>
</html>
